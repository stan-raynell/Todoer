<%= form_with(model: list) do |form| %>
  <div class="field is-grouped">
    <%= form.text_field :title, placeholder: "Add new list", :class => "input" %>
    <%= form.submit "Save", :class => "button" %>
  </div>
<% end %>